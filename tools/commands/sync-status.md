---
description: What differs between this machine and the shared vault
argument-hint: ""
---

Show the sync state without changing anything:

```bash
python3 ~/claude-code-sync/bin/ccsync.py status
```

Comment on the result in plain language: is anything waiting to be sent, are all
projects bound on this machine, which other machines are in the vault.
